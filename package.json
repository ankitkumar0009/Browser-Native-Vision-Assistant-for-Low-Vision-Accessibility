{
  "name": "browser-native-vision-assistant-for-low-vision-accessibility",
  "version": "1.0.0",
  "description": "VisionBridge AI — Browser-Native Vision Assistant",
  "main": "server/dist/index.js",
  "scripts": {
    "dev": "concurrently -n server,client \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "build": "npm --prefix server run build && npm --prefix client run build",
    "start": "npm --prefix server run start",
    "lint": "npm --prefix client run lint"
  },
  "keywords": ["accessibility", "ai", "vision", "react", "node"],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
